kind = "web"
previewPath = "/"
title = "Meta Ads Simplifier"
version = "1.0.0"
id = "artifacts/meta-report"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
localPort = 20216
name = "web"
paths = ["/"]

[services.development]
run = "pnpm --filter @workspace/meta-report run dev"

[services.env]
PORT = "20216"
BASE_PATH = "/"

[services.production]
build = ["pnpm", "--filter", "@workspace/meta-report", "run", "build"]
publicDir = "artifacts/meta-report/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"
